export enum SecurityEvents {
  PermissionRequest = "security:permission-request",
  PermissionGranted = "security:permission-granted",
  PermissionDenied = "security:permission-denied",
  Violation = "security:violation",
}
