export enum SnapshotEvents {
  Capture = "snapshot:capture",
  Captured = "snapshot:captured",
  Restore = "snapshot:restore",
  Restored = "snapshot:restored",
  List = "snapshot:list",
  TimeTravel = "snapshot:time-travel",
  Undo = "snapshot:undo",
}
